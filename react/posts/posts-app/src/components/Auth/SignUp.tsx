import {Form} from "./Form";
// import {useSignUp} from "../../hooks/UseSignUp.hook";

export const SignUp = () => {
   // const { handleSignUp } = useSignUp();

    return (
        <Form title="Sign up"/>
    )
}
