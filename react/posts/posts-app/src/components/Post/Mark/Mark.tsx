import {PostTypesEnum} from "../../../utils/globalTypes.ts";

interface Props {
    date: string,
}

export const Mark = (props: Props) => {

    return (
        <p > Marked</p>
    );
}

export default Mark;
